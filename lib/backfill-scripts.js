module.exports.runBackfillForUser = function (userId) {
  // Your backfill code would be here - something like...
  // 1. Get user object from database
  // 2. Fetch data from third party API using user's access token
  // 3. Insert data into database
  // 4. Return ok = true
  return {
    ok: true,
  };
};
